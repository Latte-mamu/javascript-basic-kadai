// 変数の宣言・値の代入(体重)
let bodyWeight = 68;

// 変数の宣言・値の代入(身長)
let height = 1.7;

// 四則演算
// console.log(bodyWeight / (height * height));

// BMI の計算結果も変数に代入
let bmi = bodyWeight / (height * height);

console.log(bmi);
